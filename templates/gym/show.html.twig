{% extends 'base.html.twig' %}

{% block title %}Gym{% endblock %}

{% block body %}
    <h1>Gym</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gym.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ gym.name }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ gym.active ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ gym.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gym_index') }}">back to list</a>

    <a href="{{ path('gym_edit', {'id': gym.id}) }}">edit</a>

    {{ include('gym/_delete_form.html.twig') }}
{% endblock %}
