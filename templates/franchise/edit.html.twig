{% extends 'base.html.twig' %}

{% block title %}Edit Franchise{% endblock %}

{% block body %}
    <h1>Edit Franchise</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('franchise_index') }}">back to list</a>

    {{ include('franchise/_delete_form.html.twig') }}
{% endblock %}
